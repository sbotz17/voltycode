# Sprint 3 — VoltyCore Foundation

Estrai questo archivio nella root del repository, poi esegui:

```bash
cd /workspaces/voltycode
chmod +x scripts/apply_sprint3.sh
./scripts/apply_sprint3.sh
```

Se i test passano:

```bash
git add -A
git commit -m "feat: add VoltyCore foundation"
git push origin main
```
